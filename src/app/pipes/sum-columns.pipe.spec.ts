import { SumColumnsPipe } from './sum-columns.pipe';

describe('SumColumnsPipe', () => {
  it('create an instance', () => {
    const pipe = new SumColumnsPipe();
    expect(pipe).toBeTruthy();
  });
});
