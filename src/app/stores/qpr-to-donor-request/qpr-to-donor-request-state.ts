const REQUEST = null

export class QprToDonorRequestState {
  request: {
    commentsMatrix: any,
    initiatedBy: any,
    reassignmentTask: any,
    sections: any,
    status: string,
    submittedAt: string,
  } = REQUEST;
}
